import React from 'react';

// Lightweight no-op component to completely eliminate background canvas CPU/GPU load on low-end devices
export const EmoticonParticles: React.FC = () => {
  return null;
};
